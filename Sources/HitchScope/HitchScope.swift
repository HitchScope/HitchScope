import Foundation

/// Entry point for configuring HitchScope in your app.
///
/// This is a scaffolding stub — real MetricKit subscription and
/// StateReporting integration land in Phase 3 of the build plan.
public struct HitchScope {
    private init() {}

    /// Configures HitchScope with your project's API key.
    public static func configure(apiKey: String) {
        // TODO(Phase 3): subscribe to MKMetricManager, wire up StateReporting,
        // and start batching/upload with retry.
    }
}
