// Copy this file to Secrets.swift (gitignored) and fill in a real key from
// a self-serve app (see hitchscope-dashboard's /apps page). Never commit
// Secrets.swift itself - this repo is public.
enum Secrets {
  static let apiKey = "hs_live_your_key_here"
}
